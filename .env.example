# OpenAI provider (default)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o
# OPENAI_BASE_URL=https://api.openai.com/v1/chat/completions  # override for Azure OpenAI or compatible services

# Local provider (Ollama, LM Studio) — no API key needed
# Set picture_description_provider="local" in PipelineConfig
